{
  "name": "make-component",
  "version": "1.0.0",
  "description": "simple code that generates the skeleton for angular 1 code",
  "main": "index.js",
  "scripts": {
    "build": "node generateScript",
    "format": "oxfmt --write .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:check": "oxfmt --check .",
    "lint": "npx oxlint ."
  },
  "author": "Sy Le",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "oxfmt": "^0.61.0"
  }
}
