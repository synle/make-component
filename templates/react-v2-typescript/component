import React, { useEffect, useState } from 'react';
import styles from './index.module.scss';

type {{UPPERFIRST_FNAME}}Props = {
}

export function {{UPPERFIRST_FNAME}}(props: {{UPPERFIRST_FNAME}}Props) : JSX.Element | null {
  const [value, setValue] = useState();

  useEffect(
    () => {

    },
    [value]
  )

  return (
    <div className={styles.{{UPPERFIRST_FNAME}}}>
      {{UPPERFIRST_FNAME}} Test
    </div>
  )
}

export default {{UPPERFIRST_FNAME}};
