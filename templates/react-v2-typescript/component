import React, { useEffect, useState } from 'react';
import styles from './index.module.scss';

type {{STARTCASE_FNAME}}Props = {
}

export function {{STARTCASE_FNAME}}(props: {{STARTCASE_FNAME}}Props) : JSX.Element | null {
  const [value, setValue] = useState();

  useEffect(
    () => {
      setValue('Some Test Value');
    },
    [value]
  )

  return (
    <div className={styles.{{STARTCASE_FNAME}}}>
      {{STARTCASE_FNAME}} Test - {value}
    </div>
  )
}

export default {{STARTCASE_FNAME}};
