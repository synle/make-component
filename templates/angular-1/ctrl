var moduleName = '{{ORIG_FNAME}}Ctrl';
module.exports = moduleName;
angular.module(moduleName, [])
    .controller(moduleName, function (
        $scope
    ) {
        $scope.some_scope_var = '123456789';
    });
