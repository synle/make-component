var moduleName = '{{ORIG_FNAME}}';
module.exports = moduleName;

// angular filter
// {{ inputValue | {{ORIG_FNAME}} }}

angular.module(moduleName, [])
    .filter(moduleName, function (

    ) {
        return function (inputValue) {
            return inputValue;
        };
    });
