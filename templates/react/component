import './{{ORIG_FNAME}}.scss';
import React, {useState, useEffect} from 'react';
import { useSelector, useDispatch } from 'react-redux';

// import * as Selectors from '../selectors';

// import * as Actions from '../actions/ReduxActions';

export default () => {
  const dispatch = useDispatch();

  // const someSelector = useSelector(Selectors.someSelector);

  // useEffect(() => {
  //   dispatch(Actions.doSomething());
  // }, [dispatch]);

  return (
    <div className='{{ORIG_FNAME}}'>
      <div>{{ORIG_FNAME}} Component</div>
      <div>Hello World</div>
    </div>
  );
};
