import React from 'react';
import { bindActionCreators } from 'redux';
import { connect } from 'react-redux';
import _ from 'lodash';

//internal
// import Action from '@src/store/Action';

const {{ORIG_FNAME}}Component = React.createClass({
  render() {
    const {content} = this.props;

    return (
      <div>
        Hello World <button onClick={this.onButtonClick}>{content}</button>
      </div>
    );
  },
  onButtonClick(){
    alert('hello world...');
  }
});


const mapStateToProps = function(state) {
  return {
    content: '{{CAMEL_FNAME}}'
    // isVisible : _.get( state, 'some_path' )
  };
}


const mapDispatchToProps = function(dispatch) {
  return {
    // onUpdate: bindActionCreators(Action.updateApiToken, dispatch)
 };
}

export default connect(mapStateToProps, mapDispatchToProps)({{ORIG_FNAME}}Component);
